# iSensor Web Demo

iSensorApp과 **같은 화면 구성·에셋·카피**로 맞춘 mock 웹 시연본입니다. (BLE/서버 없음)

## 실행

```bash
cd web-demo
npm install
npm run dev -- --host
```

브라우저에서 로컬 URL을 열거나, 같은 Wi‑Fi의 Network URL을 QR로 공유하세요.